import 'package:flutter/material.dart';

class ExcluirMembro extends StatelessWidget {
  const ExcluirMembro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
