import 'package:flutter/material.dart';

class AlterarMembro extends StatelessWidget {
  const AlterarMembro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
